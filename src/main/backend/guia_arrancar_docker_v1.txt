cd src/main/backend

hacer mvn clean para eliminar carpeta target y el jar

lanzar la web

docker run -d --name webapp12-db -e MYSQL_ROOT_PASSWORD=1234 -e MYSQL_DATABASE=Pixel_Paradise -p 3307:3306 -v C:\ruta\local\mysql-data:/var/lib/mysql mysql:8.0.33

pilla la bdd de la imagen de mysql y esto crea ademas un volumen para la persistencia

hacer mvn package para crear el jar.. rezar para que funcione si sale bien crea el archivo app.jar en la carpeta target

si sale bien hacer: docker build -t przx27/backend-app:java21 . //przx27 es mi usuario de Docker poner el vuestro habiendo hecho login al principio

si se crea la imagen la subo a Docker hub: docker push przx27/backend-app:java21  sigue en testeo que funcione bien

ahora vais a dockerdesktop y borráis la base de datos que hemos lanzado al principio(tengo que ve bien que pasa) porque ocupa el mismo puerto que cuando se crea con el Docker compose


hacemos docker-compose up --build   y si rezas lo suficiente se crea y lo puedes ver en localhost:443

no se como va lo de la api pero la web esta.


